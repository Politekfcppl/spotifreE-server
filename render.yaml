services:
  - type: web
    name: spotifreE-server
    env: node
    buildCommand: npm install && pip install yt-dlp
    startCommand: node index.js
    envVars:
      - key: NODE_VERSION
        value: 18
